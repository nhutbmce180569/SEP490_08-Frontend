import { FULL_API } from './api';

export const FINANCE_API = {
  // Stats
  GET_OPERATOR_STATS: `${FULL_API}/operator/stats`,
  
  // Withdrawals
  CREATE_WITHDRAWAL: `${FULL_API}/withdrawals`,
  GET_WITHDRAWALS: `${FULL_API}/withdrawals`,
};